<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kz.kuz.styles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FragmentApplication">
        <activity
            android:name="kz.kuz.styles.MainActivity"
            android:label="@string/main_title"
            >
        </activity>
        <activity
            android:name="kz.kuz.styles.TitleActivity"
            android:label="@string/main_title"
            android:theme="@style/Theme.TitleApplication"
            >
        </activity>
        <activity
            android:name="kz.kuz.styles.TitleActivity2"
            android:label="@string/main_title"
            android:theme="@style/Theme.TitleApplication2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>